#include <stdio.h>
#include <stdlib.h>

typedef struct {
	float x,y;
	float vx, vy;
	float m;
	float *p0, *p1, *p2, *p3; // if external branch => *p_i = NULL
} Particle;


int main(int argc, char** argv){

	return 0;
}
